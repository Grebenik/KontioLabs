/* Topic: AI Studies General — machine learning, deep learning and LLM concepts. */
SRS.registerTopic({
  id: "ai-general",
  name: "AI Studies General",
  description: "Machine learning, deep learning, and large-language-model fundamentals — from gradient descent to RAG and alignment.",
  cards: [
    { id: "ai-001", front: "What is the difference between AI, machine learning and deep learning?", back: "AI is the broad goal of machines doing intelligent tasks. ML is the subset where systems learn patterns from data instead of explicit rules. Deep learning is the subset of ML using many-layered neural networks." },
    { id: "ai-002", front: "Supervised vs unsupervised vs reinforcement learning?", back: "Supervised: learn from labeled examples (classification, regression). Unsupervised: find structure in unlabeled data (clustering, dimensionality reduction). Reinforcement: an agent learns a policy by acting in an environment and receiving rewards." },
    { id: "ai-003", front: "What is a classification vs regression problem?", back: "Classification predicts a discrete category (spam / not spam); regression predicts a continuous value (house price). The choice drives the output layer, loss function and evaluation metrics." },
    { id: "ai-004", front: "What are features and labels?", back: "Features are the input variables a model learns from; the label is the target it predicts. Feature engineering — selecting and transforming inputs — historically drove ML performance; deep learning learns features automatically." },
    { id: "ai-005", front: "What is a train/validation/test split for?", back: "Train: fit parameters. Validation: tune hyperparameters and pick models without contaminating the final estimate. Test: one honest, untouched measure of generalization. Leaking test data into training invalidates the evaluation." },
    { id: "ai-006", front: "What is overfitting and underfitting?", back: "Overfitting: the model memorizes training data (low train error, high test error). Underfitting: too simple to capture the pattern (high error everywhere). Fixes: regularization, more data, early stopping vs bigger models, better features." },
    { id: "ai-007", front: "What is the bias-variance tradeoff?", back: "Bias is error from overly simple assumptions (underfit); variance is error from sensitivity to training noise (overfit). Increasing model capacity lowers bias but raises variance — the art is balancing total error." },
    { id: "ai-008", front: "What is regularization? Give examples.", back: "Techniques that penalize complexity to improve generalization: L2 (weight decay) shrinks weights, L1 encourages sparsity, dropout randomly disables units during training, early stopping halts before overfitting, data augmentation enlarges the effective dataset." },
    { id: "ai-009", front: "What is gradient descent?", back: "Iteratively updating parameters opposite the gradient of the loss: θ ← θ − η∇L. Stochastic/mini-batch variants use subsets of data per step. The learning rate η controls step size — too high diverges, too low crawls." },
    { id: "ai-010", front: "What is backpropagation?", back: "The algorithm computing loss gradients for every weight in a network by applying the chain rule backwards from output to input — enabling gradient descent on deep networks efficiently." },
    { id: "ai-011", front: "What is a loss function? Name common ones.", back: "The objective a model minimizes: mean squared error for regression, cross-entropy for classification (and next-token prediction in LLMs), hinge loss for SVMs. The loss defines what 'good' means during training." },
    { id: "ai-012", front: "What is an activation function and why is non-linearity needed?", back: "The function applied to a neuron's weighted sum (ReLU, sigmoid, tanh, GELU). Without non-linear activations, stacked linear layers collapse into one linear map — networks couldn't learn complex functions." },
    { id: "ai-013", front: "Precision vs recall — and what is F1?", back: "Precision: of predicted positives, how many are right (TP/(TP+FP)). Recall: of actual positives, how many were found (TP/(TP+FN)). F1 is their harmonic mean — a single score balancing both when classes are imbalanced." },
    { id: "ai-014", front: "Why is accuracy misleading on imbalanced data, and what to use instead?", back: "Predicting 'no fraud' always yields 99% accuracy on a 1%-fraud dataset while being useless. Use precision/recall, F1, ROC-AUC or PR curves, and inspect the confusion matrix per class." },
    { id: "ai-015", front: "What is a confusion matrix?", back: "A table of predicted vs actual classes: true positives, false positives, false negatives, true negatives (per class). Most classification metrics (accuracy, precision, recall) are ratios read off this table." },
    { id: "ai-016", front: "What is cross-validation?", back: "Splitting data into k folds, training on k−1 and validating on the held-out fold, rotating k times, then averaging — a more reliable performance estimate than a single split, especially on small datasets." },
    { id: "ai-017", front: "What are hyperparameters vs parameters?", back: "Parameters (weights) are learned by training. Hyperparameters are set beforehand — learning rate, batch size, layer count, regularization strength — and tuned via validation performance (grid/random/Bayesian search)." },
    { id: "ai-018", front: "What is an embedding?", back: "A dense vector representing an item (word, sentence, image, user) so that semantic similarity becomes geometric closeness. Embeddings power search, recommendation, clustering, and retrieval for RAG (via cosine similarity)." },
    { id: "ai-019", front: "What is a neural network, structurally?", back: "Layers of units, each computing an activation over a weighted sum of the previous layer's outputs. Input layer → hidden layers → output layer; depth and width set capacity; weights are learned by backprop + gradient descent." },
    { id: "ai-020", front: "What are CNNs and what are they good at?", back: "Convolutional neural networks slide learned filters over grids (images), sharing weights to detect local patterns hierarchically — edges → textures → objects. Dominant in vision before transformers; still standard for many perception tasks." },
    { id: "ai-021", front: "What were RNNs/LSTMs, and what replaced them?", back: "Recurrent networks process sequences step-by-step with a hidden state; LSTMs/GRUs added gates to fight vanishing gradients. Transformers replaced them for most sequence tasks by processing all positions in parallel with attention." },
    { id: "ai-022", front: "What is the transformer architecture?", back: "A sequence model built on self-attention plus feed-forward layers, with residual connections, layer norm and positional information. Encoder/decoder or decoder-only stacks; parallelizable training made web-scale pretraining feasible ('Attention Is All You Need', 2017)." },
    { id: "ai-023", front: "What does self-attention actually compute?", back: "For each token, weighted combinations of all other tokens' values, with weights from query·key similarity (softmax(QKᵀ/√d)·V). It lets every position directly attend to relevant context regardless of distance; multiple heads learn different relations." },
    { id: "ai-024", front: "What is a large language model (LLM)?", back: "A transformer (typically decoder-only) with billions of parameters, pretrained on massive text corpora to predict the next token — from which emerge broad capabilities: writing, coding, reasoning, translation — then aligned for instruction following." },
    { id: "ai-025", front: "What is next-token prediction and why does it work so well?", back: "Training minimizes cross-entropy on predicting each token given its prefix. Doing this well at scale forces the model to internalize grammar, facts, style and reasoning patterns — compressing the training distribution into weights." },
    { id: "ai-026", front: "What are the stages of building a modern aligned LLM?", back: "1) Pretraining: next-token prediction on huge corpora. 2) Supervised fine-tuning on instruction/response data. 3) Preference optimization (RLHF, DPO, Constitutional AI) to make outputs helpful, honest, harmless. Plus evals and safety testing throughout." },
    { id: "ai-027", front: "What is RLHF?", back: "Reinforcement Learning from Human Feedback: train a reward model on human preference comparisons of outputs, then optimize the LLM against it (e.g. PPO). It aligns model behavior with human judgments beyond what supervised data captures." },
    { id: "ai-028", front: "What is fine-tuning vs prompting vs RAG for adapting an LLM?", back: "Prompting: instructions/examples in context — no training, instant iteration. RAG: retrieve external knowledge into the prompt — fresh, sourced facts. Fine-tuning: update weights on domain data — teaches style/format/skills but not reliably new facts. Start with prompting, add RAG for knowledge, fine-tune last." },
    { id: "ai-029", front: "What is retrieval-augmented generation (RAG)?", back: "Answering with retrieved evidence: chunk and embed documents, store vectors, retrieve the most similar chunks for a query, and generate an answer grounded in (and often citing) them. Cuts hallucination and keeps knowledge current without retraining." },
    { id: "ai-030", front: "What is a vector database?", back: "A store optimized for similarity search over embeddings using approximate nearest neighbor indexes (HNSW, IVF), with metadata filtering — the retrieval backbone of RAG and semantic search (e.g. pgvector, Pinecone, FAISS-based systems)." },
    { id: "ai-031", front: "What is hallucination in LLMs and how do you mitigate it?", back: "Confidently generated false content — a byproduct of predicting plausible text. Mitigations: grounding with RAG and citations, letting the model say 'I don't know', verification steps or tool use for facts and math, and human review for high stakes." },
    { id: "ai-032", front: "What is in-context learning / few-shot prompting?", back: "The model adapts to a task purely from examples placed in the prompt — no weight updates. Zero-shot: instruction only; few-shot: several input→output demonstrations, which strongly shape format and accuracy." },
    { id: "ai-033", front: "What is chain-of-thought reasoning?", back: "Having the model produce intermediate reasoning steps before its answer, which measurably improves multi-step problems. Modern models formalize this as built-in 'thinking' phases; the reasoning tokens cost latency but buy accuracy." },
    { id: "ai-034", front: "What is temperature in LLM sampling?", back: "A scale on the output distribution before sampling: low temperature sharpens it (deterministic-ish, repetitive), high flattens it (diverse, riskier). Related: top-p (nucleus) sampling keeps the smallest set of tokens covering probability p. (Newest Claude models drop these knobs in favor of prompting.)" },
    { id: "ai-035", front: "What is a context window and what happens when it's exceeded?", back: "The maximum tokens the model attends to per request. Overflow must be handled by the application: truncation, summarization/compaction, retrieval of only relevant history, or hierarchical memory. Long contexts also cost more and can dilute attention." },
    { id: "ai-036", front: "What is quantization of model weights?", back: "Storing weights in fewer bits (FP16 → INT8/INT4) to shrink memory and speed inference with modest quality loss. Enables running large models on smaller hardware; common schemes include GPTQ, AWQ and llama.cpp's GGUF formats." },
    { id: "ai-037", front: "What is distillation?", back: "Training a smaller 'student' model to reproduce a larger 'teacher' model's outputs (soft labels or generated data), transferring much of the capability at a fraction of the inference cost." },
    { id: "ai-038", front: "What is LoRA / parameter-efficient fine-tuning?", back: "Instead of updating all weights, train small low-rank adapter matrices added to frozen layers. Orders of magnitude fewer trainable parameters — cheap task adaptation, swappable adapters, less catastrophic forgetting." },
    { id: "ai-039", front: "What is Mixture of Experts (MoE)?", back: "An architecture where a router activates only a few expert sub-networks per token, so total parameters are huge but per-token compute stays modest — better capability per FLOP (e.g. Mixtral, several frontier models)." },
    { id: "ai-040", front: "What are scaling laws?", back: "Empirical power laws relating loss to model size, data and compute. Chinchilla's key result: for a compute budget, balance parameters and training tokens (~20 tokens/parameter) — many earlier models were undertrained for their size." },
    { id: "ai-041", front: "What is an AI agent?", back: "An LLM in a loop with tools: it observes state, reasons, chooses actions (tool calls), and iterates toward a goal — versus a single call or a code-orchestrated workflow. Agents suit open-ended tasks where the path can't be pre-scripted." },
    { id: "ai-042", front: "What is tool use / function calling?", back: "The model emits structured calls to developer-defined functions (name + JSON arguments); the application executes them and returns results for the model to continue with. It grounds LLMs in real systems: search, databases, code execution, APIs." },
    { id: "ai-043", front: "What is prompt injection?", back: "Adversarial instructions hidden in content the model processes (web pages, emails, documents) that try to override its actual instructions — the LLM analog of SQL injection. Mitigations: privilege separation, treating retrieved content as data, output filtering, human approval for sensitive actions." },
    { id: "ai-044", front: "What is jailbreaking vs prompt injection?", back: "Jailbreaking: the user tries to trick the model past its own safety training. Prompt injection: third-party content hijacks the model's instructions against the user/developer. Different threat actors, overlapping defenses." },
    { id: "ai-045", front: "What is the alignment problem?", back: "Making AI systems robustly pursue intended goals and human values — beyond surface obedience: avoiding reward hacking, deception, and harmful instrumental behavior as capabilities grow. Techniques include RLHF, Constitutional AI, interpretability and evals." },
    { id: "ai-046", front: "What is Constitutional AI?", back: "Anthropic's method where the model critiques and revises its own responses against explicit written principles, and preferences are generated by AI feedback (RLAIF) guided by that constitution — scalable alignment with inspectable values." },
    { id: "ai-047", front: "What is interpretability research?", back: "Understanding what happens inside models: which features/circuits produce behaviors (mechanistic interpretability), e.g. sparse autoencoders extracting human-legible features. Goals: debugging, auditing safety-relevant computation, and trust." },
    { id: "ai-048", front: "What are evals / benchmarks for LLMs?", back: "Standardized measurements of capability and safety: MMLU (knowledge), GSM8K/MATH (math), HumanEval/SWE-bench (coding), plus red-teaming and domain evals. Production teams build task-specific evals — benchmark scores don't guarantee your use case works." },
    { id: "ai-049", front: "What is an LLM-as-judge evaluation?", back: "Using a strong model to grade outputs against a rubric — scalable, cheap evaluation for style/correctness where exact matching fails. Watch for biases (position, verbosity, self-preference) and calibrate against human labels." },
    { id: "ai-050", front: "What is multimodality?", back: "Models that consume and/or produce multiple modalities — text, images, audio, video. Vision-language models encode images into tokens the transformer attends over, enabling OCR, chart reading, UI understanding and visual reasoning." },
    { id: "ai-051", front: "What is tokenization and why does it matter?", back: "Splitting text into subword units (BPE and variants) that the model actually processes. It sets effective context length and cost, explains oddities (character counting, rare-word splits), and differs per model family — count with the model's own tokenizer." },
    { id: "ai-052", front: "What is the difference between training compute and inference compute?", back: "Training: enormous one-time cost to learn weights (GPU-months at scale). Inference: per-request cost to run the model, dominated by memory bandwidth (KV cache) and output length. 'Thinking' models shift extra compute to inference time for better reasoning." },
    { id: "ai-053", front: "What is the KV cache in transformer inference?", back: "Cached key/value tensors for already-processed tokens so each new token attends without recomputing the whole prefix — making generation O(n) per token instead of O(n²), at the price of significant GPU memory that grows with context length." },
    { id: "ai-054", front: "What is data leakage in ML?", back: "Information from outside the training scope (test rows, future data, target-derived features) contaminating training — inflating measured performance that collapses in production. Prevent with strict temporal/entity splits and pipeline discipline." },
    { id: "ai-055", front: "What is drift in deployed ML systems?", back: "Data drift: input distribution shifts from training. Concept drift: the input→label relationship changes. Both silently degrade models — monitor input stats and live performance, retrain or recalibrate on schedule or trigger." },
    { id: "ai-056", front: "What is MLOps?", back: "Engineering practice for the ML lifecycle: versioned data/models/experiments, automated training and deployment pipelines, monitoring for drift and quality, rollback strategies — DevOps discipline applied to models." },
    { id: "ai-057", front: "What is synthetic data in LLM training?", back: "Model-generated training data — instruction pairs, reasoning traces, critiques — used to augment scarce human data (e.g. distillation, self-improvement loops, Constitutional AI feedback). Quality control matters: training on your own unfiltered outputs degrades models." },
    { id: "ai-058", front: "What are diffusion models?", back: "Generative models that learn to reverse gradual noising: starting from noise, they iteratively denoise toward a sample, usually guided by a text prompt (Stable Diffusion, DALL·E, Midjourney). Dominant for image/video generation; LLM-style autoregression dominates text." },
    { id: "ai-059", front: "What is AGI, and how do frontier labs frame it?", back: "Hypothetical AI matching or exceeding humans across most economically valuable cognitive work. Labs frame progress via capability evals and safety levels (e.g. Anthropic's AI Safety Levels / responsible scaling policies) that gate deployment on demonstrated risks." },
    { id: "ai-060", front: "Name the main axes to compare LLM providers/models on.", back: "Capability on YOUR tasks (own evals), context window, latency and throughput, cost per token, tool/agent support, safety behavior, data privacy/retention terms, deployment options (API, cloud marketplaces, on-prem/open weights)." }
  ]
});
