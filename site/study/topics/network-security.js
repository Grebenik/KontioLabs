/* Topic: Network Security — firewalls, IDS/IPS, segmentation and zero trust,
   VPN/PKI/TLS, common network attacks and their mitigations, hardening,
   monitoring, and cloud network security. Built for students preparing for
   a network-security-focused certification exam (e.g. Security+, Network+
   security domain, CCNA Security). Complements the general Networking
   Studies deck, which covers protocols/addressing rather than security. */
SRS.registerTopic({
  id: "network-security",
  name: "Network Security",
  description: "Certification exam prep for network security: firewalls, IDS/IPS, segmentation and zero trust, VPN/PKI/TLS, common attacks and mitigations, hardening, and monitoring.",
  cards: [
    { id: "nsc-001", front: "What is 'defense in depth'?", back: "Layering multiple, redundant security controls (physical, perimeter, network, host, application, data) so that the failure of any single control doesn't result in a full compromise — no one layer is trusted to be sufficient alone." },
    { id: "nsc-002", front: "How does the CIA triad apply specifically to network security?", back: "Confidentiality: encrypt traffic in transit (TLS/VPN). Integrity: detect tampering (hashing, signed protocols, IDS). Availability: redundancy and DDoS/flood defenses keep the network reachable under attack or failure." },
    { id: "nsc-003", front: "What is a DMZ (screened subnet), and what belongs in it?", back: "A network segment between untrusted (internet) and trusted (internal) zones, hosting public-facing services (web, mail, DNS) that must be reachable externally — isolating them so a compromise there doesn't directly expose the internal network." },
    { id: "nsc-004", front: "What is a packet-filtering firewall?", back: "A stateless firewall that allows/denies traffic based on layer 3/4 header fields (source/destination IP, port, protocol) via an ACL — fast and simple, but has no awareness of connection state or application content." },
    { id: "nsc-005", front: "What is a stateful firewall, and why is it stronger than packet filtering?", back: "It tracks a connection-state table and only allows response traffic that matches an established session, blocking unsolicited inbound traffic by default — far harder to bypass than static packet-filter rules alone." },
    { id: "nsc-006", front: "What is an application/proxy firewall?", back: "A firewall that inspects layer 7 content, terminating and re-establishing each connection itself — it can enforce protocol compliance and content rules, at the cost of higher latency and resource use than stateful inspection." },
    { id: "nsc-007", front: "What is a Next-Generation Firewall (NGFW)?", back: "A firewall combining stateful inspection with application awareness, integrated IPS, user-identity awareness, and often TLS inspection — the modern default for perimeter firewalls." },
    { id: "nsc-008", front: "Default-deny vs default-allow — which is best practice, and why?", back: "Default-deny: block everything, explicitly allow only required traffic. It's the security best practice because unknown/future traffic is blocked by default; default-allow requires anticipating every threat to block, which never keeps up." },
    { id: "nsc-009", front: "How does firewall rule order affect behavior?", back: "Most firewalls apply first-match logic — more specific rules must precede general ones, or the general rule wins first. An implicit deny is applied last, after all explicit rules, on a default-deny firewall." },
    { id: "nsc-010", front: "IDS vs IPS — what's the core architectural difference?", back: "An IDS passively monitors a copy of traffic (tap/SPAN port) and alerts — it can't block. An IPS sits inline in the traffic path and can drop malicious traffic in real time, at the risk that false positives block legitimate traffic." },
    { id: "nsc-011", front: "Signature-based vs anomaly-based detection — trade-offs?", back: "Signature-based matches known attack patterns: low false positives, but blind to zero-days/novel attacks. Anomaly/behavior-based baselines normal traffic and flags deviations: catches novel attacks, but generates more false positives that need tuning." },
    { id: "nsc-012", front: "Why segment a network for security, beyond performance?", back: "Segmentation limits the blast radius of a compromise (lateral movement is contained), isolates sensitive systems (e.g. cardholder data, OT) from general traffic, and lets different segments carry different security policies." },
    { id: "nsc-013", front: "What is microsegmentation?", back: "Fine-grained, often per-host/per-workload security policy — commonly in a data center or cloud environment — enforced independent of physical network topology. It's a key building block of zero trust architectures." },
    { id: "nsc-014", front: "What is the core principle of Zero Trust?", back: "'Never trust, always verify': no implicit trust is granted based on network location (being 'inside' the perimeter). Every request is authenticated, authorized and encrypted, continuously, regardless of where it originates." },
    { id: "nsc-015", front: "Name the typical pillars of a Zero Trust architecture.", back: "Identity, device, network/segmentation, application, and data — each verified continuously with least-privilege access, operating under the assumption that a breach has already occurred somewhere in the environment." },
    { id: "nsc-016", front: "Walk through 802.1X NAC roles.", back: "Supplicant (the device requesting access), authenticator (the switch/AP enforcing the port), and authentication server (RADIUS, validating credentials). The port stays closed except to EAP traffic until authentication succeeds." },
    { id: "nsc-017", front: "What does NAC posture assessment add beyond simple 802.1X authentication?", back: "Checking a device's security state — patch level, antivirus status, disk encryption — before or alongside authentication, and routing failing devices to a quarantine/remediation VLAN instead of full network access." },
    { id: "nsc-018", front: "What is a bastion host (jump box)?", back: "A hardened, closely monitored system that serves as the single point of entry for administrative access into a protected network segment — reducing the number of systems directly exposed to management traffic." },
    { id: "nsc-019", front: "What is Privileged Access Management (PAM)?", back: "Vaulting and rotating administrative credentials, recording privileged sessions, and granting just-in-time elevated access instead of standing admin rights — reduces the value/lifespan of any single stolen credential." },
    { id: "nsc-020", front: "Split tunneling vs full tunneling on a VPN — security trade-off?", back: "Split tunneling routes only corporate-destined traffic through the VPN (better performance) but leaves the rest of the user's traffic outside corporate security controls. Full tunneling routes everything through the VPN — more secure, more bandwidth/latency cost." },
    { id: "nsc-021", front: "AH vs ESP in IPsec — which is actually used?", back: "AH provides integrity/authentication only (no confidentiality) and is rarely deployed. ESP provides encryption plus optional authentication and is what's almost universally used in real IPsec deployments." },
    { id: "nsc-022", front: "What happens in IKE Phase 1 vs Phase 2?", back: "Phase 1 establishes a secure, authenticated channel (an ISAKMP SA) between the two peers. Phase 2 negotiates the actual IPsec SAs — the parameters used to encrypt real traffic — inside that already-secured channel." },
    { id: "nsc-023", front: "SSL/TLS VPN vs IPsec VPN — how do they differ operationally?", back: "SSL/TLS VPNs are often clientless or thin-client, easier to pass through firewalls (port 443), and typically grant app/portal-level access. IPsec VPNs work at the network level, usually need client software, and are common for site-to-site or full network access." },
    { id: "nsc-024", front: "What makes WireGuard notable among VPN protocols?", back: "A small, auditable codebase using modern cryptography by default, UDP-based, with much simpler configuration than IPsec — driving growing enterprise and consumer adoption." },
    { id: "nsc-025", front: "Which VPN protocols are considered deprecated/insecure, and why?", back: "PPTP (broken MS-CHAP-based encryption) and plain L2TP without IPsec (no native encryption of its own) — L2TP is only secure when wrapped in IPsec." },
    { id: "nsc-026", front: "What is the chain of trust in PKI?", back: "A Certificate Authority (CA) issues and signs certificates binding a public key to an identity. Trust flows from a root CA through intermediate CAs down to leaf/end-entity certificates; operating systems and browsers ship a trusted root store to anchor this chain." },
    { id: "nsc-027", front: "CRL vs OCSP vs OCSP stapling — how do they differ?", back: "CRL: a downloadable list of revoked certificates, which can be stale. OCSP: a real-time per-certificate revocation check against the CA. OCSP stapling: the server proactively attaches a signed OCSP response, avoiding a client-to-CA round trip." },
    { id: "nsc-028", front: "Outline the TLS handshake at a high level.", back: "Client Hello (supported versions/ciphers) → Server Hello + certificate → key exchange → both sides derive session keys → encrypted application data flows. TLS 1.3 shortens this to fewer round trips than TLS 1.2." },
    { id: "nsc-029", front: "What is (perfect) forward secrecy, and how is it achieved?", back: "A property where compromising a server's long-term private key later does NOT let an attacker decrypt previously captured traffic, because session keys were derived using ephemeral Diffie-Hellman (DHE/ECDHE) rather than the long-term key directly." },
    { id: "nsc-030", front: "Which SSL/TLS versions are deprecated, and what's the current minimum?", back: "SSLv2, SSLv3, TLS 1.0 and TLS 1.1 all have known weaknesses and are deprecated. TLS 1.2 is the practical minimum today, with TLS 1.3 preferred for its improved security and performance." },
    { id: "nsc-031", front: "What is certificate pinning, and its trade-off?", back: "An app hardcodes/expects a specific certificate or public key for a domain rather than trusting any CA-issued certificate, mitigating a compromised or rogue CA. Trade-off: it breaks the app on legitimate certificate rotation if pins aren't updated in step." },
    { id: "nsc-032", front: "How does ARP spoofing enable a man-in-the-middle attack?", back: "The attacker sends forged ARP replies associating their own MAC with another host's IP (often the gateway), redirecting that traffic through themselves. Mitigations: Dynamic ARP Inspection (DAI), static ARP entries for critical hosts." },
    { id: "nsc-033", front: "What is MAC flooding, and how is it mitigated?", back: "Overwhelming a switch's CAM table with bogus source MACs so it fails open and floods all traffic to every port like a hub, letting an attacker sniff it. Mitigated by port security limiting the number of MACs learned per port." },
    { id: "nsc-034", front: "What is VLAN hopping, and its two main techniques?", back: "Double-tagging (exploiting native-VLAN handling on trunk ports to smuggle a frame into another VLAN) and switch spoofing (pretending to be a trunking switch via DTP). Mitigated by disabling DTP, avoiding VLAN 1 as the native VLAN, and explicitly tagging the native VLAN." },
    { id: "nsc-035", front: "DHCP spoofing vs DHCP starvation — what's the difference?", back: "Spoofing: a rogue DHCP server hands out a malicious gateway/DNS to victims. Starvation: an attacker exhausts the address pool with bogus requests, denying service to legitimate clients. Both are mitigated by DHCP snooping (trusting only designated ports)." },
    { id: "nsc-036", front: "What is DNS spoofing/cache poisoning, and a key mitigation?", back: "Injecting forged DNS responses so a resolver caches an attacker-controlled IP for a legitimate name. Mitigated by DNSSEC (signed responses), randomized query IDs/source ports, and encrypted DNS (DoH/DoT) against on-path tampering." },
    { id: "nsc-037", front: "What is session hijacking, and how is it prevented?", back: "Stealing or predicting a valid session token/cookie to impersonate an authenticated user — often via sniffing unencrypted traffic or XSS. Prevented by TLS everywhere, secure/HttpOnly cookies, and short session lifetimes." },
    { id: "nsc-038", front: "What is an on-path (man-in-the-middle) attack, generally?", back: "Intercepting or altering traffic between two parties who believe they're communicating directly. ARP spoofing, rogue Wi-Fi access points, and DNS spoofing are common ways an attacker gets into that position." },
    { id: "nsc-039", front: "How does a SYN flood work, and how is it mitigated?", back: "The attacker sends many spoofed SYNs that never complete the handshake, exhausting the server's half-open connection table and denying service to real clients. Mitigated by SYN cookies and connection rate limiting." },
    { id: "nsc-040", front: "Name the three DDoS attack categories and give an example of each.", back: "Volumetric (saturate bandwidth — e.g. UDP floods), protocol (exhaust connection state — e.g. SYN flood), and application-layer (exhaust app resources with legitimate-looking requests — e.g. HTTP flood). Each needs different mitigation: scrubbing, rate limiting, or a WAF." },
    { id: "nsc-041", front: "What is an amplification/reflection attack?", back: "Spoofing the victim's source IP in small requests to servers that reply with much larger responses (DNS, NTP, memcached), multiplying attack bandwidth toward the victim. Mitigated by disabling open resolvers/recursion and ingress/egress filtering (BCP38) to stop source-spoofed traffic leaving a network." },
    { id: "nsc-042", front: "What is a smurf attack specifically?", back: "A form of ICMP amplification: a spoofed-source ping is sent to a subnet's broadcast address, so every host on that subnet replies to the victim at once. Mitigated by disabling directed broadcast on routers." },
    { id: "nsc-043", front: "Rogue access point vs evil twin — what's the difference?", back: "A rogue AP is any unauthorized access point on the network. An evil twin specifically impersonates a legitimate SSID to capture credentials/traffic from clients that connect to it by mistake. Wireless IDS/IPS (WIPS) and 802.1X mutual authentication mitigate both." },
    { id: "nsc-044", front: "What is a deauthentication attack, and what mitigates it?", back: "Forging 802.11 management (deauth) frames to disconnect clients — for denial of service, or to force a reconnect and capture the handshake for offline cracking. Management Frame Protection (802.11w) mitigates it." },
    { id: "nsc-045", front: "What weakness does WPA3's SAE handshake fix from WPA2?", back: "WPA2's 4-way handshake is vulnerable to offline dictionary attacks against a weak pre-shared key (and to protocol-level attacks like KRACK). WPA3's SAE is a password-authenticated key exchange resistant to offline dictionary attacks and provides forward secrecy." },
    { id: "nsc-046", front: "EAP-TLS vs PEAP/EAP-TTLS — how do they differ?", back: "EAP-TLS uses mutual certificate-based authentication — the strongest option, but requires a full PKI to issue client certificates. PEAP/EAP-TTLS wrap a weaker inner authentication method (like a password) inside an outer TLS tunnel, easing deployment where client certs aren't practical." },
    { id: "nsc-047", front: "What is switch port security, and what actions can it take?", back: "Restricting which/how many MAC addresses may appear on a switch port (e.g. via sticky MAC learning), with a configurable violation action — shutdown, restrict, or protect — the front-line defense against MAC flooding and unauthorized device connections." },
    { id: "nsc-048", front: "Why disable unused ports and services even with no known vulnerability?", back: "Reducing attack surface is valuable on its own: an unused switch port or unnecessary listening service (Telnet, unneeded management protocols, CDP/LLDP where not required) is one less thing that could be discovered and abused later." },
    { id: "nsc-049", front: "What defines secure device management access?", back: "SSH instead of Telnet, HTTPS instead of HTTP for admin interfaces, and ideally an out-of-band management network physically/logically separate from production data traffic — so a data-plane compromise can't reach device consoles." },
    { id: "nsc-050", front: "Why does accurate NTP matter for network security specifically?", back: "Synchronized time is required for meaningful log correlation across devices, for certificate validity checking, and for protocols like Kerberos — attacking or misconfiguring NTP can quietly undermine all of those." },
    { id: "nsc-051", front: "What does a SIEM add that individual device logs don't provide alone?", back: "It aggregates and correlates logs/events from firewalls, IDS/IPS, servers and endpoints across the whole network, surfacing incidents that no single device's logs would reveal in isolation, and centralizes alerting and retention for forensics/compliance." },
    { id: "nsc-052", front: "What is a honeypot/honeynet, and why is any interaction with it significant?", back: "A decoy system (or network of them) made to look like a real target, isolated from production, meant to attract and study attacker behavior. Because no legitimate user has any reason to touch it, any interaction is inherently a high-confidence alert." },
    { id: "nsc-053", front: "What is a UTM appliance, and its main trade-off?", back: "A Unified Threat Management device bundling firewall, IPS, antivirus/anti-malware, content filtering and VPN into one box — simple to manage for smaller networks, but a single point of failure and potential performance bottleneck." },
    { id: "nsc-054", front: "What does a WAF protect against that a network firewall can't?", back: "Application-layer web attacks — SQL injection, XSS, path traversal — by inspecting HTTP(S) request/response content. A network firewall only sees IP/port-level traffic and has no visibility into this layer." },
    { id: "nsc-055", front: "What is SSL/TLS inspection, and its trade-off?", back: "A proxy terminates and re-establishes TLS to inspect content, requiring a trusted internal CA certificate on managed devices. It gives visibility into encrypted threats but breaks certificate pinning, adds latency/complexity, and is itself a controlled man-in-the-middle that must be tightly governed." },
    { id: "nsc-056", front: "Why are air-gapped/isolated networks still used for OT/ICS environments?", back: "Industrial control systems (SCADA, ICS) often can't be patched or secured like IT systems, so physical/logical isolation from IT and the internet — sometimes via one-way data diodes — compensates by removing the network path an attacker would need." },
    { id: "nsc-057", front: "East-west vs north-south traffic — why does the distinction matter for security design?", back: "North-south is traffic crossing the network perimeter (the classic firewall's job). East-west is traffic between systems inside the network/data center — historically under-inspected once past the perimeter, which is exactly why segmentation, microsegmentation and zero trust specifically target it." },
    { id: "nsc-058", front: "What is the cloud shared responsibility model, security-wise?", back: "The cloud provider secures the underlying infrastructure (physical, hypervisor, global network); the customer is responsible for configuring their own network controls correctly — security groups, NACLs, IAM. Most cloud breaches trace to customer misconfiguration, not provider failure." },
    { id: "nsc-059", front: "Security groups vs NACLs — how do they combine for defense in depth?", back: "Security groups are stateful (return traffic is automatically allowed) and apply per-instance. NACLs are stateless (both directions must be explicitly allowed) and apply per-subnet as a coarser additional layer — used together, not as alternatives to each other." },
    { id: "nsc-060", front: "What administrative controls keep technical network security controls from drifting out of sync with reality?", back: "A documented least-privilege access policy, a current network diagram/asset inventory, formal change management for firewall/ACL changes, and periodic access reviews — without these, technical controls silently rot as the network evolves." }
  ]
});
