/* Topic: Anthropic AI Architecture Exam Study — Claude models, Messages API,
   tool use, MCP, agents, caching, context management, platform operations
   (Console/Admin/Usage APIs, versioning, data retention), agent security,
   and Anthropic's safety framework (RSP/ASL, Constitutional AI/classifiers).
   Built for students preparing for an Anthropic/Claude platform certification
   exam. Accurate as of mid-2026 — verify exact model names, prices and beta
   header strings against current docs before an exam or production use. */
SRS.registerTopic({
  id: "anthropic-architecture",
  group: "Technology & Security",
  name: "Anthropic AI Architecture",
  description: "Certification exam prep for the Claude platform: models, Messages API, tool use, MCP, agents, prompt caching, context management, platform operations, agent security, and Anthropic's safety framework.",
  cards: [
    { id: "aa-001", front: "What is the single core endpoint of the Claude API, and what goes through it?", back: "POST /v1/messages — the Messages API. Chat, vision, tool use, structured outputs and server-side tools are all features of this one endpoint, supported by helper endpoints (batches, files, token counting, models)." },
    { id: "aa-002", front: "Name the current Claude model tiers and what each is for.", back: "Fable/Mythos (Claude 5 family): most capable, hardest reasoning and long-horizon agentic work. Opus: most capable general tier for demanding work. Sonnet: best balance of intelligence, speed and cost. Haiku: fastest and cheapest for simple, high-volume tasks." },
    { id: "aa-003", front: "What is a context window?", back: "The maximum number of tokens (input + output) the model can attend to in one request. Current Claude flagship models offer a 1M-token context window; Haiku 4.5 offers 200K. The API is stateless — you resend the conversation every turn." },
    { id: "aa-004", front: "Roughly how do tokens relate to text?", back: "A token is a sub-word unit — roughly 3–4 English characters or ~0.75 words on average. Code, non-English text and unusual formatting tokenize less efficiently. Use the count_tokens endpoint for exact, model-specific counts." },
    { id: "aa-005", front: "What are the required parameters of a Messages API call?", back: "model, max_tokens, and messages (an array of {role, content} turns starting with a user message). system, tools, thinking, output_config and stream are common optional parameters." },
    { id: "aa-006", front: "What is the system prompt and how does it differ from user messages?", back: "A top-level 'system' parameter that sets persistent role, rules and context with operator-level authority. It renders before the conversation, is ideal for caching, and isn't part of the alternating user/assistant turns." },
    { id: "aa-007", front: "Why must you resend the full conversation history on every Claude API call?", back: "The Messages API is stateless — the server keeps no session. Each request must include all prior turns; prompt caching makes re-sending the shared prefix cheap." },
    { id: "aa-008", front: "What is extended/adaptive thinking?", back: "The model reasons in dedicated thinking blocks before answering. On modern Claude models this is 'adaptive' — the model decides when and how much to think — replacing the older fixed token budget (budget_tokens). Thinking improves hard reasoning at the cost of latency and tokens." },
    { id: "aa-009", front: "What is the effort parameter?", back: "output_config.effort (low/medium/high/xhigh/max) controls how much reasoning and token spend the model applies. Higher effort → deeper thinking, more tool calls, higher quality on hard tasks; lower effort → faster, cheaper, more scoped work." },
    { id: "aa-010", front: "List the main stop_reason values and their meaning.", back: "end_turn (finished naturally), max_tokens (hit the output cap — likely truncated), stop_sequence (hit a custom stop string), tool_use (model wants a tool executed), pause_turn (server-tool loop paused, re-send to resume), refusal (declined for safety — check stop_details)." },
    { id: "aa-011", front: "How does streaming work in the Claude API?", back: "Set stream: true and the response arrives as Server-Sent Events: message_start, content_block_start, content_block_delta (text chunks), content_block_stop, message_delta (stop_reason, usage), message_stop. Streaming is required for very large max_tokens to avoid HTTP timeouts." },
    { id: "aa-012", front: "What is the tool use flow, step by step?", back: "1) Send tools (name, description, JSON-Schema input_schema). 2) Model responds with stop_reason 'tool_use' and tool_use block(s) with id, name, input. 3) You execute the tool. 4) Append the assistant turn plus a user turn containing tool_result blocks (matching tool_use_id). 5) Repeat until end_turn." },
    { id: "aa-013", front: "What are the tool_choice options?", back: "auto (model decides — default), any (must use at least one tool), tool + name (must use that specific tool), none (no tools). Any of them can add disable_parallel_tool_use: true to force one call per response." },
    { id: "aa-014", front: "How should parallel tool calls be handled?", back: "One assistant message can carry multiple tool_use blocks. Execute them (ideally concurrently) and return ALL tool_result blocks in a single user message; splitting them across messages trains the model to stop parallelizing. Failed tools get is_error: true, not silence." },
    { id: "aa-015", front: "Server-side vs client-side tools — what's the difference?", back: "Server-side tools (web search, web fetch, code execution) run on Anthropic's infrastructure; you just declare them and results appear in the response. Client-side tools (your custom tools, bash, text editor, computer use, memory) are executed by your code, which returns tool_result blocks." },
    { id: "aa-016", front: "What does the server-side code execution tool provide?", back: "A sandboxed container (Python, no internet) where Claude runs code automatically — data analysis, file processing, chart generation. Containers persist ~30 days and can be reused across requests via container_id to keep files and state." },
    { id: "aa-017", front: "What is stop_reason 'pause_turn' and how do you handle it?", back: "The server-side tool loop hit its iteration limit mid-turn. Re-send the conversation with the paused assistant turn appended and the server resumes automatically — don't inject an artificial 'Continue' user message." },
    { id: "aa-018", front: "What is MCP (Model Context Protocol)?", back: "An open protocol standardizing how AI applications connect to external tools and data. MCP servers expose tools/resources/prompts; clients (Claude apps, IDEs, the API's MCP connector) consume them — one integration works across many hosts." },
    { id: "aa-019", front: "How does the Claude API's MCP connector work?", back: "Pass mcp_servers: [{type:'url', url, name}] plus a tools entry {type:'mcp_toolset', mcp_server_name: <name>} (beta header required). Claude then calls the remote MCP server's tools server-side during the response." },
    { id: "aa-020", front: "What is prompt caching, fundamentally?", back: "A prefix match: the rendered prompt (tools → system → messages) is cached up to cache_control breakpoints. Later requests reusing the identical byte prefix pay ~0.1× for cached tokens. Any byte change invalidates everything after it." },
    { id: "aa-021", front: "What are the economics of prompt caching?", back: "Cache reads cost ~0.1× the base input price. Writes cost a premium: 1.25× for the 5-minute TTL, 2× for the 1-hour TTL. Break-even is about two requests (5m) or three (1h). Up to 4 breakpoints per request." },
    { id: "aa-022", front: "Name three silent prompt-cache invalidators.", back: "Timestamps or UUIDs interpolated into the system prompt; non-deterministic serialization (unsorted JSON keys); changing/reordering the tool list (tools render first, so everything invalidates). Also: switching models — caches are model-scoped." },
    { id: "aa-023", front: "How do you verify prompt caching is working?", back: "Check response usage: cache_read_input_tokens (served from cache), cache_creation_input_tokens (written this request), input_tokens (uncached). Zero cache reads across identical requests means a silent invalidator is changing the prefix." },
    { id: "aa-024", front: "What is the Message Batches API?", back: "Asynchronous bulk processing: submit up to 100K requests, poll until processing_status is 'ended', then stream per-request results keyed by custom_id (order not guaranteed). 50% price discount; most batches finish within an hour (24h max)." },
    { id: "aa-025", front: "What is the Files API for?", back: "Upload a file once (PDF, image, data), get a file_id, and reference it in many message requests without re-uploading — as document/image content blocks or container uploads for code execution." },
    { id: "aa-026", front: "What are structured outputs?", back: "Constraining the response to a JSON Schema via output_config.format (type json_schema) — guaranteeing parseable output — and/or strict: true on tool definitions to guarantee tool inputs validate exactly against the schema." },
    { id: "aa-027", front: "What replaced assistant-message prefilling on modern Claude models?", back: "Prefilling (seeding the last assistant turn) now returns a 400. Use structured outputs for format control, system-prompt instructions for style ('respond without preamble'), and enum tool fields for classification." },
    { id: "aa-028", front: "What are the four criteria for deciding to build an agent (vs a workflow)?", back: "Complexity (multi-step, hard to fully specify), Value (outcome justifies cost/latency), Viability (the model is good at this task type), Cost of error (mistakes are catchable/recoverable). Any 'no' → use a simpler tier: single call or code-orchestrated workflow." },
    { id: "aa-029", front: "Workflow vs agent — what's the architectural difference?", back: "In a workflow, your code owns the control flow and calls the model at fixed steps. In an agent, the model drives an open-ended loop — deciding which tools to call and when to stop — while your harness executes the calls." },
    { id: "aa-030", front: "What are the four ways to build an agent on the Claude platform?", back: "1) Manual loop over /v1/messages (you own everything). 2) SDK Tool Runner (SDK runs the loop over your tools). 3) Managed Agents (Anthropic hosts the loop AND a per-session sandbox). 4) Claude Agent SDK (the Claude Code harness with built-in tools, self-hosted)." },
    { id: "aa-031", front: "In Managed Agents, what are Agent, Session, Environment and Container?", back: "Agent: persisted, versioned config (model, system, tools, MCP, skills). Session: one stateful run referencing an agent + environment, producing an event stream. Environment: reusable container template. Container: the isolated workspace where the agent's tools execute." },
    { id: "aa-032", front: "Why are Managed Agents created once and referenced by ID?", back: "Agents are versioned, persistent objects: sessions pin to a version, so you can update prompts/tools without breaking running sessions, roll back regressions, and reproduce runs. Creating an agent per request orphans objects and defeats versioning." },
    { id: "aa-033", front: "When should an action be promoted from bash to a dedicated tool in an agent harness?", back: "When the harness needs a typed hook: gating destructive actions behind approval, staleness checks on edits, custom UI rendering, auditing, or safe parallel scheduling. Bash gives breadth but only an opaque command string." },
    { id: "aa-034", front: "What are Agent Skills?", back: "Packaged folders of task-specific instructions (SKILL.md plus files) that load progressively — the description sits in context, the full content loads only when relevant. They turn general agents into specialists without bloating every prompt." },
    { id: "aa-035", front: "Compare the three context-management tools for long-running agents.", back: "Context editing: clears stale tool results/thinking blocks (prunes, doesn't summarize). Compaction: server-side summarization into a compaction block when nearing the context limit — you must echo it back. Memory: files that persist across sessions." },
    { id: "aa-036", front: "What is the critical rule when using server-side compaction?", back: "Append the full response.content (including compaction blocks) back into your message history. Extracting only the text silently drops the compaction state the server needs to replace the summarized history." },
    { id: "aa-037", front: "What is programmatic tool calling (PTC)?", back: "Claude writes a script in the code-execution container that calls your tools as functions; intermediate results go to the script, not the context window. Great for chains of many calls or large intermediate data — only the final output returns to Claude." },
    { id: "aa-038", front: "What is the tool search tool?", back: "A server-side tool that lets Claude discover relevant tools from a large library at runtime. Other tools are marked defer_loading: true; discovered schemas are appended (not swapped), which preserves the prompt cache." },
    { id: "aa-039", front: "How does vision input work in the Messages API?", back: "Image content blocks with a base64, URL or file_id source placed in user messages (before related text). Claude reads charts, screenshots and photos; image tokens scale with resolution." },
    { id: "aa-040", front: "What does the refusal stop_reason mean and how should production code handle it?", back: "The model or safety classifiers declined the request; content may be empty or partial. Always branch on stop_reason before reading content, surface the refusal, and don't blind-retry the identical prompt (fallback-to-another-model patterns exist for false positives)." },
    { id: "aa-041", front: "Which HTTP errors should be retried, and how?", back: "429 (rate limit), 500, 529 (overloaded) and network errors — with exponential backoff and jitter; honor the retry-after header. Don't retry 400/401/403/404. The official SDKs retry automatically (default 2 attempts)." },
    { id: "aa-042", front: "What rate-limit dimensions does the Claude API enforce?", back: "Requests per minute (RPM) and input/output tokens per minute (ITPM/OTPM), per organization and model, by usage tier. Responses carry x-ratelimit-* headers and retry-after on 429." },
    { id: "aa-043", front: "Why should max_tokens be generous, and what forces streaming?", back: "max_tokens is a hard cap on output (thinking included on thinking models) — too low truncates mid-answer. Very large values (beyond ~16K) require streaming so the HTTP connection doesn't time out while the model generates." },
    { id: "aa-044", front: "What are the three required headers on a raw Claude API request?", back: "x-api-key: <key>, anthropic-version: 2023-06-01, Content-Type: application/json. Beta features add anthropic-beta: <flag>. OAuth flows use Authorization: Bearer instead of x-api-key." },
    { id: "aa-045", front: "What is the Models API used for?", back: "GET /v1/models lists models; GET /v1/models/{id} returns display name, context window (max_input_tokens), output cap (max_tokens) and a capabilities tree (vision, thinking modes, effort, structured outputs) — live capability discovery instead of hardcoding." },
    { id: "aa-046", front: "What is token counting and why not use tiktoken?", back: "POST /v1/messages/count_tokens returns exact, model-specific token counts for a prospective request. tiktoken is OpenAI's tokenizer and undercounts Claude tokens by ~15–20%+ — estimates from it are simply wrong for Claude." },
    { id: "aa-047", front: "Name the classic prompt-engineering techniques that work well with Claude.", back: "Be explicit and give the motivation behind instructions; use XML tags to structure input; provide few-shot examples; give Claude room to reason (thinking); assign a role via the system prompt; for long docs, put instructions after the document." },
    { id: "aa-048", front: "What is the memory tool?", back: "A client-side tool (memory_20250818) where Claude reads/writes files under a /memories directory through commands (view, create, str_replace, insert, delete, rename). You implement the storage; it gives agents state across sessions." },
    { id: "aa-049", front: "What is computer use?", back: "A tool family where Claude operates a GUI: it receives screenshots and issues mouse/keyboard actions your harness executes. Used for browser/desktop automation; resolution and screenshot cadence drive cost and accuracy." },
    { id: "aa-050", front: "What secures MCP/agent credentials in Managed Agents?", back: "Vaults: Anthropic-stored credentials (OAuth with auto-refresh, static bearers, environment variables substituted at egress). The sandbox never sees the real secret — even injected prompts can't exfiltrate it. Never put keys in system prompts or messages." },
    { id: "aa-051", front: "What is the difference between temperature-style sampling control and modern Claude models?", back: "Recent Claude models (Opus 4.7+, Sonnet 5, Fable) removed temperature/top_p/top_k — steering is done by prompting and the effort parameter. Older models accept temperature 0–1 (one of temperature or top_p, not both on Claude 4+)." },
    { id: "aa-052", front: "What are stop sequences?", back: "Custom strings (stop_sequences parameter) that end generation when the model emits them; the response reports stop_reason 'stop_sequence' and which one fired. Useful to cut output at a known delimiter." },
    { id: "aa-053", front: "How do you build a simple RAG flow with the Claude API?", back: "Retrieve relevant chunks (vector/keyword search — external, since Anthropic doesn't host embeddings), pack them into the prompt as structured context (often with citations enabled on document blocks), and instruct Claude to answer only from the provided sources." },
    { id: "aa-054", front: "What are citations in the Claude API?", back: "Enable citations on document content blocks and the response text is split into blocks carrying citation arrays — cited_text plus location (character range, page number, or content block index) — so answers can be traced to sources." },
    { id: "aa-055", front: "Where can Claude models be consumed besides the first-party API?", back: "Claude Platform on AWS (Anthropic-operated, full parity, SigV4 auth), Amazon Bedrock (anthropic.-prefixed model IDs), Google Vertex AI, and Microsoft Foundry. Feature availability varies by platform — check before porting code." },
    { id: "aa-056", front: "What is the Claude Agent SDK and how does it differ from the API Tool Runner?", back: "The Agent SDK is Claude Code packaged as a library: full harness with built-in file/bash/search tools, subagents, hooks and permissions — you host it. The Tool Runner is a thin SDK helper that just loops over tools YOU define on the Messages API." },
    { id: "aa-057", front: "What safety layers does Anthropic apply to Claude deployments?", back: "Usage policies, model training (Constitutional AI / RLHF), safety classifiers on inputs/outputs for high-risk domains (which can return refusal stop reasons), and platform-level enforcement. Applications add their own guardrails and human oversight." },
    { id: "aa-058", front: "What is Constitutional AI, at a high level?", back: "Anthropic's training approach where the model critiques and revises its own outputs against an explicit set of principles (a 'constitution'), reducing reliance on human labels for harmlessness and making values inspectable." },
    { id: "aa-059", front: "Multi-agent design: what's the recommended orchestrator/subagent pattern?", back: "An orchestrator delegates independent subtasks to parallel subagents (each with scoped context and tools) and integrates results — asynchronously where possible so long-lived agents keep their context. Fan out for independent work; stay single-agent for sequential dependent steps." },
    { id: "aa-060", front: "How should an application decide between Haiku, Sonnet and Opus-tier models?", back: "Match capability to task and volume: Haiku for simple, fast, high-volume classification/routing; Sonnet for most production workloads (balanced cost/intelligence); Opus/Fable for the hardest reasoning, long-horizon agents and highest-stakes quality. Evaluate empirically — don't guess." },

    { id: "aa-061", front: "What is interleaved thinking?", back: "A beta capability (interleaved-thinking header) letting Claude produce thinking blocks BETWEEN tool calls within one turn — reasoning about a tool's result before deciding the next action — instead of only thinking once before the first tool call." },
    { id: "aa-062", front: "Why must a thinking block's content be replayed unmodified when continuing a tool-use turn?", back: "Each thinking block carries a signature cryptographically tied to its content; the API validates it on replay. Editing the thinking text or dropping the signature invalidates it and the request errors — thinking blocks must be echoed back byte-for-byte, not summarized." },
    { id: "aa-063", front: "What is a redacted_thinking block?", back: "When internal safety systems flag a thinking block's content, the API returns it encrypted rather than in plain text. It still must be passed back in later turns like any other thinking block — the model can use its own prior reasoning, but your app/logs never see the plaintext." },
    { id: "aa-064", front: "What does Priority service tier provide, versus standard?", back: "service_tier: 'priority' (or 'auto') reserves throughput for paying customers, giving lower and more consistent latency under load, at a premium price — versus the default best-effort standard tier. Useful for latency-sensitive production traffic." },
    { id: "aa-065", front: "Dated model snapshots vs unversioned aliases — why does it matter?", back: "Model IDs are available as dated snapshots (e.g. claude-sonnet-5-20260115) for reproducibility, and often as an unversioned alias that resolves to the latest snapshot. Production systems typically pin the dated snapshot and upgrade deliberately, rather than being silently moved by an alias." },
    { id: "aa-066", front: "How does Anthropic handle model deprecation?", back: "Deprecation dates and retirement windows are published in advance (typically months of notice) on the model deprecations page. A pinned dated snapshot keeps working until its retirement date — teams must track announcements and migrate before the cutoff." },
    { id: "aa-067", front: "What is the Admin API for?", back: "A separate, organization-scoped API (not for calling Claude itself) for programmatically managing an org: creating/rotating workspace API keys, inviting or removing members, and managing workspaces — platform automation rather than model usage." },
    { id: "aa-068", front: "What is the Usage & Cost API for?", back: "Endpoints returning historical token usage and spend broken down by API key, workspace, model and time period — for building internal dashboards or chargeback reporting without scraping the Console UI." },
    { id: "aa-069", front: "What are Workspaces in the Console, and why use them?", back: "Logical groupings (e.g. by team, project or environment) inside one organization, each with their own API keys and optional spend limits — so usage, billing and access can be isolated per workspace." },
    { id: "aa-070", front: "Does Anthropic train on API inputs/outputs by default?", back: "No. API conversations are not used to train models by default. Data is retained briefly for abuse monitoring/trust & safety purposes and then deleted per the retention policy; eligible enterprise customers can negotiate Zero Data Retention." },
    { id: "aa-071", front: "What is data residency, and why check it?", back: "Some enterprise agreements and cloud platforms (e.g. regional AWS/GCP deployments) offer guarantees about which region processes/stores data. The first-party API's default processing region should be checked against any regulatory/compliance requirement before sending regulated data." },
    { id: "aa-072", front: "How does Claude actually read a PDF?", back: "Via document content blocks (base64, URL, or file_id): Claude processes both the extracted text AND the rendered page images, so it can read charts, layouts and scanned pages — not just plain text extraction. Per-request page-count limits apply." },
    { id: "aa-073", front: "What are the practical limits on vision input?", back: "Multiple images per request are supported, but count and total size are limited per request/model; more or larger images cost more tokens and add latency. There is no native video input — video must be sampled to still frames first." },
    { id: "aa-074", front: "What does the web_search server-side tool do, and how do you scope it?", back: "Lets Claude issue live web searches and read results with citations, without you implementing search yourself. Scope it with allowed/blocked domain lists and a max-uses-per-turn cap to bound cost and keep results on-topic." },
    { id: "aa-075", front: "How does web_fetch differ from web_search?", back: "web_search discovers pages for a query; web_fetch retrieves and reads the content of one specific URL (found via search, or supplied by the user/you) — the 'open this link' step rather than the 'find pages about X' step." },
    { id: "aa-076", front: "What is the text editor tool?", back: "A client-side tool family (str_replace-based commands: view, create, str_replace, insert) giving Claude structured file-editing operations that your harness executes against real files — the same primitive Claude Code's Edit tool is built on." },
    { id: "aa-077", front: "What is the bash tool, and what's its trade-off vs a purpose-built tool?", back: "A client-side tool letting Claude run arbitrary shell commands your harness executes and returns output for. It's broad and flexible but opaque — see aa-033 for when to promote a bash action to a dedicated, typed tool instead." },
    { id: "aa-078", front: "What are Constitutional classifiers?", back: "Input/output safety classifiers trained to detect and block specific harm categories (e.g. CBRN uplift) with low added latency and false-positive rate, deployed at runtime alongside Constitutional AI training (aa-058) — notably hardening against jailbreak and many-shot jailbreak attacks." },
    { id: "aa-079", front: "What is the Responsible Scaling Policy (RSP) and AI Safety Levels (ASL)?", back: "Anthropic's public framework tying increasing model capability to increasing required safeguards (ASL-2, ASL-3, ...). Defined evaluations must pass before training or deploying a more capable model — the governance backbone behind Anthropic's safety commitments." },
    { id: "aa-080", front: "What are Model Cards / System Cards?", back: "Publicly released documents accompanying major model releases, detailing capabilities, evaluated risks (e.g. cybersecurity, bio, persuasion), safety testing performed, and intended use — the primary transparency artifact for a given model version." },
    { id: "aa-081", front: "What is Anthropic's Usage Policy, and who does it bind?", back: "The acceptable-use rules for the API and products (prohibiting things like weapons development, CSAM, and election manipulation). Violations can be enforced via classifiers, account review, or suspension. Applications deploying Claude must comply and typically add their own guardrails on top." },
    { id: "aa-082", front: "Claude.ai vs the Claude Developer Platform — what's the distinction?", back: "Claude.ai is the consumer/team chat product (web, desktop, mobile). The Developer Platform (API + Console) is what you integrate programmatically. Features can ship on one before the other — check which surface a capability is actually available on." },
    { id: "aa-083", front: "How does Claude Code relate to the Claude Agent SDK?", back: "Claude Code is Anthropic's own coding-agent product. The Claude Agent SDK packages that same underlying harness — tools, permissions, subagents — as a library, so developers can build their own Claude-Code-style agents for domains beyond coding." },
    { id: "aa-084", front: "What's the common pattern behind Claude in Chrome, Excel, Slack, etc.?", back: "Anthropic and partners embed Claude directly inside existing tools (browser extension, productivity apps, chat platforms) so users get agentic help without leaving their workflow — a third mode alongside claude.ai and the raw API." },
    { id: "aa-085", front: "What is the message role-alternation rule?", back: "The messages array must strictly alternate user/assistant turns (after any leading user message). Consecutive same-role content should be merged into one message with multiple content blocks, not sent as separate turns." },
    { id: "aa-086", front: "Why is a message's 'content' an array of blocks rather than one string?", back: "So a single turn can mix content types — text, image, document, tool_use, tool_result, thinking — in order. That's what lets one assistant turn contain reasoning, a tool call, and later a text answer, all as ordered blocks." },
    { id: "aa-087", front: "How is the very large (1M token) context window enabled?", back: "It's gated behind a beta header (context-1m) on supported models/tiers, not on by default. Pricing for tokens beyond a threshold (e.g. 200K) is often higher — enable it deliberately for workloads that actually need it." },
    { id: "aa-088", front: "What is 'context rot' in long-context usage?", back: "Model quality on information placed in the middle of a very long context can degrade even within the stated window ('lost in the middle'). Long-context apps should still prioritize relevant material near the start/end and use retrieval to trim filler, rather than relying on window size alone." },
    { id: "aa-089", front: "What does building evals for a Claude-powered app involve?", back: "A representative test set with expected outputs or rubrics, run against candidate prompts/models, scored automatically (exact match, rubric-based grading, or human review) before shipping prompt or model changes — treating prompts like code that needs regression tests." },
    { id: "aa-090", front: "What is the LLM-as-judge pattern, and its main caveat?", back: "Using a (often stronger) Claude call to grade another Claude call's output against a rubric — useful when correctness isn't a simple string match. It needs its own calibration (checking agreement against human judgments) to be trustworthy; an uncalibrated judge just adds a second unreliable opinion." },
    { id: "aa-091", front: "What usually has more cost impact: prompt wording or these levers?", back: "Right-sizing the model per task (aa-060), prompt caching for repeated prefixes, batching non-interactive workloads, capping max_tokens sensibly, and trimming tool schemas/context typically dominate cost — micro-optimizing prompt wording is a smaller lever." },
    { id: "aa-092", front: "How strong is Claude's multilingual support, and what's the token cost?", back: "Claude models handle many languages beyond English, though performance and tokenization efficiency are strongest in English and other high-resource languages. Non-English text generally consumes more tokens per word (see aa-004)." },
    { id: "aa-093", front: "What does 'defense in depth' look like for content moderation in your own app?", back: "Beyond Anthropic's built-in safety classifiers and refusals: input validation, a moderation pass on user content, output filtering, logging/monitoring, and a human-review path for edge cases — no single layer should be relied on alone." },
    { id: "aa-094", front: "What is Zero Data Retention (ZDR), and what's the trade-off?", back: "An enterprise agreement option where Anthropic does not retain API inputs/outputs beyond the request lifecycle, for customers with strict compliance needs. It typically disables features that require retention — like prompt caching or the Batches polling window — so weigh the trade-off." },
    { id: "aa-095", front: "What most commonly causes wrong or missed tool calls?", back: "Vague tool or parameter descriptions, more so than model choice. Tool-calling accuracy is highly sensitive to clear, specific descriptions of what a tool does, when to use it, and its edge cases." },
    { id: "aa-096", front: "What is input_schema on a tool definition, and what's still your job?", back: "Standard JSON Schema (type, properties, required, enums, etc.) describing the tool's expected arguments; the model constructs a JSON object matching it. You must still validate/handle it defensively unless you've opted into strict mode (aa-026)." },
    { id: "aa-097", front: "What happens if a tool_use block doesn't get a matching tool_result?", back: "The API errors. Every tool_use block in the prior assistant turn needs a corresponding tool_result in the next user turn, matched by tool_use_id — even if that means returning an error-flagged result for a tool you chose not to actually run." },
    { id: "aa-098", front: "How should very large tool outputs be handled before returning them to Claude?", back: "Summarize or truncate them in your harness first. Dumping raw megabytes of file contents or query output into a tool_result wastes context and tokens and can crowd out the actual task." },
    { id: "aa-099", front: "What's the difference between a Skill and a tool?", back: "A tool is a callable function Claude can invoke with structured input. A Skill (aa-034) is a bundle of instructions/context — and optionally its own tools/scripts — that teaches Claude HOW to approach a class of task, often orchestrating several tool calls under a learned procedure." },
    { id: "aa-100", front: "What role do subagents play in a multi-agent harness?", back: "Scoped, often stateless worker agents an orchestrator spins up for a bounded task (e.g. 'search the codebase for X'). They return a result and their exploration context disappears, keeping the orchestrator's own context window from growing with detail it doesn't need (aa-059)." },
    { id: "aa-101", front: "What is prompt injection, and why does it matter more for agents?", back: "Untrusted content (web pages, files, tool outputs) containing instructions that try to hijack the model's behavior. Agents with tool access are higher-stakes targets than plain chat, because a successful injection can trigger real actions — file writes, purchases, data exfiltration — not just a bad reply." },
    { id: "aa-102", front: "What defenses reduce prompt-injection risk in an agent harness?", back: "Least-privilege tool scoping, human confirmation before high-impact actions, sandboxing (no real credentials in reach — see the Vaults pattern, aa-050), monitoring outputs/actions, and clearly framing untrusted content so the model can distinguish it from operator instructions. No single layer is sufficient alone." },
    { id: "aa-103", front: "Does the system prompt provide an absolute guarantee against injected instructions?", back: "No. System-prompt instructions carry more authority than user-turn or untrusted tool/document content by design and training, but this is a strong prior, not a hard guarantee — which is why agent harnesses still need the defenses in aa-102." },
    { id: "aa-104", front: "What commonly limits Claude on long-horizon agentic tasks?", back: "Context growth from accumulated tool-call history, compounding small errors over many steps, and losing track of the original goal — addressed by context editing/compaction (aa-035), subagent delegation (aa-100), and periodic self-checks/plans, rather than by raw model capability alone." },
    { id: "aa-105", front: "Why might a production system route requests to different effort levels instead of fixing one?", back: "Higher effort/thinking spend improves accuracy on hard reasoning but adds latency and cost on every call, including easy ones. Routing by task difficulty (a cheap classifier or heuristic) typically beats applying one fixed effort level to all requests." },
    { id: "aa-106", front: "What should you double-check before trusting count_tokens for capacity planning?", back: "That the counted request exactly matches the real one — same tools, system prompt, and images. Token counts depend on the full rendered prompt, not just the message text; vision and tool schemas can add non-obvious overhead." },
    { id: "aa-107", front: "What is a 'container' in the code-execution / Managed Agents context?", back: "An isolated sandboxed execution environment (filesystem, no general internet by default) where server-side code runs or a Managed Agent's tools operate. Containers can persist and be reused via an ID across requests/sessions to keep files and installed state (aa-016, aa-031)." },
    { id: "aa-108", front: "How should secrets be supplied to tools an agent calls?", back: "Never inline in the system prompt or messages — the model could reveal them, or a prompt injection could exfiltrate them. Use short-lived scoped credentials in your own execution environment, or Vaults in Managed Agents (aa-050); the model should reference a credential by name, not hold its value." },
    { id: "aa-109", front: "How do a refusal and a low-quality answer differ, and why does the difference matter operationally?", back: "A refusal has stop_reason 'refusal' — a deliberate policy decision (aa-040). A low-quality answer completes normally (end_turn) but is simply wrong or shallow. They need different fixes: surface/escalate the policy issue vs. improve the prompt, context, or effort level." },
    { id: "aa-110", front: "What's the right way to stay current on exact model names, prices and beta headers for this exam?", back: "Treat them as reference-lookup facts, not things to over-memorize — Anthropic ships models, betas and API changes frequently. The release notes/changelog and model/system cards are the authoritative, current source; know the concepts here, then verify specifics before using them." }
  ]
});
